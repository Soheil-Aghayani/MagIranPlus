HOST=0.0.0.0
PORT=5000
MAGIRAN_ALLOWED_ORIGINS=https://soheil-aghayani.github.io,http://127.0.0.1:5000,http://localhost:5000
# Optional administrator-owned HTTP(S) proxy for the API server only.
# Do not place a visitor's V2Ray link here and never expose this value to the browser.
MAGIRAN_EGRESS_PROXY=
